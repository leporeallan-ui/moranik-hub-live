{
  "name": "moranik-hub",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "build:prod": "npm run build && mkdir -p backend/public && powershell -Command \"Copy-Item -Path 'dist\\*' -Destination 'backend\\public\\' -Recurse -Force\"",
    "start:backend": "cd backend && npm install --production && npm start"
  },
  "dependencies": {
    "react-dom": "^19.2.3",
    "react": "^19.2.3",
    "@google/genai": "^1.37.0"
  },
  "devDependencies": {
    "@types/node": "^22.14.0",
    "@vitejs/plugin-react": "^5.0.0",
    "typescript": "~5.8.2",
    "vite": "^6.2.0"
  }
}
